// import sensor from '@ohos.sensor';
//
// // 注册心率监听器
// const heartRateSensor = sensor.getSensor(sensor.SensorId.HEART_RATE);
// heartRateSensor.on('change', (data) => {
//   console.log(`心率: ${data.value} bpm`);
// });
//
// // 计步器实现
// const pedometer = sensor.getSensor(sensor.SensorId.PEDOMETER);
// pedometer.on('change', (data) => {
//   this.steps = data.steps;
// });
//
